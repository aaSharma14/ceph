import { PermissionScopeDirective } from './permission-scope.directive';
describe('PermissionScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionScopeDirective();
    expect(directive).toBeTruthy();
  });
});
